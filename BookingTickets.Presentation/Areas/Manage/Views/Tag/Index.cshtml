@model List<TagReturnDto>
@{
    int count = 0;
}
<div class="container">
    <div class="row">
        <a asp-action="Create" class="btn btn-outline-success">Create</a>
    </div>
    <div class="row">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Count</th>
                    <th scope="col">Name</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tag in Model)
                {
                    <tr>
                        <td>@(++count)</td>
                        <td>@Html.Raw(tag.Name)</td>
                        <td>
                            <a class="btn btn-primary" asp-action="Update" asp-route-id="@tag.Id">Edit</a>
                            <a class="btn btn-primary" asp-action="detail" asp-route-id="@tag.Id">Detail</a>
                            <a class="btn btn-primary" asp-action="Delete" asp-route-id="@tag.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>